/*
Find errors, if any, in each of the following looping segments. Assume that all the variables have been declared 
and assigned values.
(c) do;
    total = total + value;
    scanf("%f", &value);
    while (value != 999);
*/
#include<stdio.h>

int main(){
    // Error(s):
    // 1. There should be curly braces after "do" and not semicolon(";"), meaning the two lines of block of code should be enclosed witin the curly braces.
    
        return 0;
}