/*
State errors, if any, in the following input statements
(d) scanf(\n"%f",root);
*/
#include<stdio.h>

int main(){
    float root;
       scanf("%f",&root);         //We have to use "address of operator" i.e. "&" to read a character through scanf since we have to provide the address of the variable in order to store a variable in it.
       
                                
    return 0;
}