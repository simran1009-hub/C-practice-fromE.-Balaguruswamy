//Q. Write a program to convert a given character to its ASCII value and vice versa.
#include<stdio.h>

int main(){
    char c='A';
    printf("The ascii value of the value in character variable c is %d\n",c);
    printf("The charcater corresponding to the ascii value %d is %c\n",c,c);
    return 0;
}