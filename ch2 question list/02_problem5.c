//Q. Write a program to demonstrate pre-increment and post-increment operators.
#include<stdio.h>

int main(){
    int i=4;
           printf("The value of i after using the pre-increment operator is %d\n",++i);
           i=4;
           printf("The value of i after using the post-increment operator is %d\n",i++);

return 0;
}